# Binaries & executables
/bin/
/server
/agent
probe-server
probe-agent
*.exe
*.exe~
*.dll
*.so
*.dylib

# SQLite databases & temp files
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Node dependencies
node_modules/
web/node_modules/
web/dist/
*.tsbuildinfo

# OS and IDE files
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~
.gemini/

# Logs
*.log
logs/

# Local environment files. docker-compose reads .env, and the example asks the
# operator to put the admin password there — so it must never be committed.
# The template itself is negated back in, since it carries no real values.
.env
.env.*
!.env.example
